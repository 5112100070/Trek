<nav class="navbar navbar-expand static-top" style="background-color:#febe2f;" >
  <a class="navbar-brand mr-1" style="color:black;" href="#" onClick="javascript:GoToIndex('dashboard')"><img src="/etc/favicon.ico" width="15%" height="15%" /> Dashboard Trek</a>
  <!-- Navbar -->
  <ul class="navbar-nav ml-auto d-none d-md-inline-block form-inline ml-auto mr-0 mr-md-3 my-2 my-md-0">
    <li class="nav-item dropdown no-arrow">
      <a class="nav-link dropdown-toggle text-dark" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        {{ .UserDetail.GetFullname }} <i class="fas fa-user-circle fa-fw"></i>
      </a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
        <a class="dropdown-item" href="#">Settings</a>
        <a class="dropdown-item" href="#">Activity Log</a>
        <div class="dropdown-divider"></div>
        <a class="dropdown-item" href="#" data-toggle="modal" data-target="#logoutModal">Logout</a>
      </div>
    </li>
  </ul>
</nav>

<!-- Logout Modal-->
<div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="label-logout-modal" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="label-logout-modal">Ingin Keluar ?</h5>
        <button class="close" type="button" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">×</span>
        </button>
      </div>
      <div class="modal-body">Pilih "Logout" jika ingin mengakhiri session</div>
      <div class="modal-footer">
        <button class="btn btn-secondary" type="button" data-dismiss="modal">Batal</button>
        <a class="btn btn-primary" onClick="javascript:ProcessLogout()">Logout</a>
      </div>
    </div>
  </div>
</div>

<!-- Modal -->
<div id="loading-pop-up" class="modal-loading">
    <div class="lds-facebook"><div></div><div></div><div></div></div>
</div>

<script>
    function StartLoading(){
        document.getElementById('loading-pop-up').style.display = "block";
    }
    function FinishLoading(){
        document.getElementById('loading-pop-up').style.display = "none";
    }
</script>