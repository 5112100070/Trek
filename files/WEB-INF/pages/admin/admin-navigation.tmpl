<!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light static-top">
      
        <a style="cursor: pointer;" onClick="javascript:GoToIndex('')">
            <img src="/img/logo-trek.png" width="100" height="35" class="d-inline-block align-top" alt="" style="margin-right:20px;">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
      
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
              <li class="nav-item" style="margin:0 10px 0; cursor: pointer;">
                <a class="nav-link js-scroll-trigger" onClick="javascript:GoToIndex('admin/product')"><b>Produk</b></a>
              </li>
          </ul>

          {{ if (not .UserDetail) }}
            <a class="btn btn-trek" style="color: rgba(14, 14, 14, 0.68); font-weight:bolder; margin:0 10px 0;" onClick="javascript:GoToIndex('login')">LOGIN</a>
          {{ else }}
            <a class="btn btn-trek" style="color: rgba(14, 14, 14, 0.68); font-weight:bolder; margin:0 10px 0;" onClick="javascript:ProcessLogout()">LOGOUT</a>
          {{ end }}
        </div>
      </nav>